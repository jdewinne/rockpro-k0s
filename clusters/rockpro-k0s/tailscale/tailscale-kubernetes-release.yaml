apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-kubernetes
  namespace: tailscale
spec:
  interval: 5m
  chart:
    spec:
      chart: tailscale-operator
      version: '1.94.2'
      sourceRef:
        kind: HelmRepository
        name: tailscale-kubernetes
        namespace: tailscale
      interval: 1m